Crea un componente React para: $ARGUMENTS

Pasos:
1. Componente funcional en /frontend/src/components/
2. Props tipadas con TypeScript interface
3. Estilos con clases Tailwind (no CSS inline)
4. Si necesita datos del servidor: usar React Query + hook propio en /hooks/
5. Si tiene estado global: conectar con Zustand store correspondiente
6. Soporte dark/light mode usando las CSS variables del tema
